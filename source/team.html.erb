<h2 class="text-center padded">Our team</h2>

<ul class="list-inline text-center">
  <% ["cherine", "amelie", "buddy"].each do |member| %>
    <li>
      <%= image_tag "#{member}.png", class: "img-circle avatar-large" %>
      <h3><%= member.capitalize %></h3>
    </li>
  <% end %>
</ul>
